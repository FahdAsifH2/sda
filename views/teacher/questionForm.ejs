<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit Question</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f6f9;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            justify-content: center;
            align-items: center;
        }

        /* Header styling */
        .header {
            background-color: #2c3e50;
            color: white;
            width: 100%;
            padding: 20px;
            text-align: center;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 10;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h1 {
            margin-bottom: 20px;
            font-size: 26px;
            color: #2c3e50;
            text-align: center;
        }

        .container {
            margin-top: 80px; /* To ensure content stays below the header */
            width: 100%;
            max-width: 800px;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        label {
            font-size: 16px;
            color: #333;
            margin-bottom: 8px;
            display: block;
        }

        textarea, input[type="text"], select {
            width: 100%;
            padding: 12px;
            margin: 10px 0 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
            color: #333;
        }

        textarea {
            resize: vertical;
        }

        select {
            cursor: pointer;
        }

        button {
    display: block;
    width: 100%;
    background-color: #3498db;
    color: white;
    padding: 15px;
    font-size: 16px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.3s ease; /* Added smooth transition */
}

button:hover {
    background-color: #1abc9c; /* New color on hover */
    transform: scale(1.05); /* Slight enlargement on hover */
}


        .form-group {
            margin-bottom: 20px;
        }

        /* Responsive design */
        @media (max-width: 600px) {
            .container {
                padding: 20px;
            }

            h1 {
                font-size: 22px;
            }

            textarea, input[type="text"], select, button {
                font-size: 14px;
                padding: 10px;
            }
        }

    </style>
</head>
<body>
    <!-- Fixed Header -->
    <div class="header">
        <h2>PrepMaster - Submit Question</h2>
    </div>

    <!-- Main form container -->
    <div class="container">
        <h1>Submit a Question</h1>
        <form action="/teacher/submit-question" method="POST">
            <div class="form-group">
                <label for="question">Question:</label>
                <textarea name="question" rows="4" required></textarea>
            </div>

            <div class="form-group">
                <label for="optionA">Option A:</label>
                <input type="text" name="optionA" required>
            </div>

            <div class="form-group">
                <label for="optionB">Option B:</label>
                <input type="text" name="optionB" required>
            </div>

            <div class="form-group">
                <label for="optionC">Option C:</label>
                <input type="text" name="optionC" required>
            </div>

            <div class="form-group">
                <label for="optionD">Option D:</label>
                <input type="text" name="optionD" required>
            </div>

            <div class="form-group">
                <label for="correctOption">Correct Option:</label>
                <select name="correctOption" required>
                    <option value="A">A</option>
                    <option value="B">B</option>
                    <option value="C">C</option>
                    <option value="D">D</option>
                </select>
            </div>

            <button type="submit">Submit Question</button>
        </form>
    </div>
</body>
</html>
